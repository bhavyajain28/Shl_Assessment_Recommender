# CPU-only torch build: plain PyPI `torch` also pulls several NVIDIA CUDA
# packages as dependencies on Linux (nvidia-cublas, nvidia-cudnn, etc, each
# hundreds of MB) even though we never use a GPU. Pinning the +cpu wheel from
# PyTorch's own index avoids all of that -- crucial for fitting Azure App
# Service's disk/memory limits.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.1+cpu

fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
python-dotenv==1.0.1
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
openai==1.58.1
sentence-transformers==3.3.1
faiss-cpu==1.9.0
numpy==1.26.4
tenacity==9.0.0
rank-bm25==0.2.2
pytest==8.3.4
httpx==0.28.1
